@model IEnumerable<Garbage.Common.DTO.AddDumpedAreaDTO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="card-header">
    <strong class="card-title">Registered Areas</strong>
</div>
<div class="card-body">

</div><div class="row">
    <div class="col-sm-12">
        <table id="bootstrap-data-table" class="table table-striped table-bordered dataTable no-footer" role="grid" aria-describedby="bootstrap-data-table_info">
            <thead>
                <tr style="background-color:#d39e00">
                    <th>
                        Name
                    </th>
                    @*<th>
                            @Html.DisplayNameFor(model => model.createdby)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.updatedby)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.createdtime)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.updatedtime)
                        </th>*@
                    <th>
                        Address
                    </th>
                    <th>
                        City
                    </th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)

                    </td>
                    @*<td>
            @Html.DisplayFor(modelItem => item.createdby)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.updatedby)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.createdtime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.updatedtime)
        </td>*@
                    <td>
                        @Html.DisplayFor(modelItem => item.source)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.cityid_fk) @*need to show city name*@
                    </td>
                    
                    <td class="text-center">
                        <a class='btn btn-info btn-xs' href="@Url.Action("Edit","Complaints",new { id = item.Dumping_id })new { id = item.Dumping_id })"><i class="fa fa-edit"></i><span class="glyphicon glyphicon-edit"></span> Edit</a>
                        <a class="btn btn-danger btn-xs" href="@Url.Action("Delete","Complaints",new { id = item.Dumping_id })"><i class="ti-close"></i><span class="glyphicon glyphicon-remove"></span> Del</a>
                    </td>
                  
                  
                </tr>
                }

            </tbody>
        </table>
    </div>
</div>
<style>
    .btn-xs {
        border: 1px solid;
        border-radius: 10px;
    }
</style>